services:
  frontend:
    build:
      context: ./frontend 
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./backend   
      dockerfile: Dockerfile
    ports:
      - "1234:1234"
    volumes:
      - ./database:/backend/database
    environment:
      - HOST=0.0.0.0
      - PORT=1234
      - YPERSISTENCE=./database